import { SetImageSizeDirective } from './set-image-size.directive';

describe('SetImageSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new SetImageSizeDirective();
    expect(directive).toBeTruthy();
  });
});
